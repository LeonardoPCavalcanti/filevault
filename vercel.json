{
  "buildCommand": "npm run build --workspace=@filevault/shared && npm run build --workspace=@filevault/web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm ci --include=dev",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
